{
  "name": "high-availability-api",
  "version": "1.0.0",
  "description": "Simple Node.js API to demonstrate High Availability on ECS Fargate",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
